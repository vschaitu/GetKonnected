import { container, title } from "../mainStyle.js";

const pillsStyle = {
  section: {
    padding: "0 0"
  },
  container,
  title: {
    ...title,
    marginTop: "0px",
    minHeight: "32px",
    textDecoration: "none"
  }
};

export default pillsStyle;
